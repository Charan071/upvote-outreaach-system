# upvote-outreaach-system

Contact pool: Product Hunt makers → LinkedIn outreach (Next.js, Prisma/SQLite, Unipile, Gemini, Context.dev).

Secrets stay in `.env` locally and in Railway variables. Never commit `.env`.
